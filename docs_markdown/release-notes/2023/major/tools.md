# Improvements to GROMACS tools

## `gmx do_dssp` replaced by native implementation of DSSP algorithm

`gmx do_dssp` replaced by native implementation of DSSP algorithm,
version 4. Results for version 4 was formerly available with
`gmx do_dssp -ver 4`. The new tool is called `gmx dssp`.
